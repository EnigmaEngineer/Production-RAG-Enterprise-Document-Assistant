apiVersion: v1
kind: Secret
metadata:
  name: {{ include "rag.fullname" . }}-secrets
  labels:
    {{- include "rag.labels" . | nindent 4 }}
type: Opaque
stringData:
  RAG_API_KEY: {{ .Values.secrets.apiKey | quote }}
  HF_TOKEN: {{ .Values.secrets.hfToken | quote }}
